function lcs(s){let r=s.reduce((e,l)=>e.length<=l.length?e:l);var o=r.length;for(let l=o;0<=l;l--)for(let e=0;e<=o-l;e++){var c=r.substring(e,e+l);if(s.every(e=>~e.indexOf(c)))return c}return""}let b=0;if(process.argv.length<3)console.log("");else if(process.argv.slice(2).length<=64){for(let e=1;e<process.argv.slice(2).length;e++)process.argv.slice(2)[e].length<=256&&(b+=1);(b=process.argv.slice(2).length)?console.log(lcs(process.argv.slice(2))):console.log("\n")}